import React from 'react';

const CountDown = () => {
    return(
        <div>
            <h1>
                There are {this.state} days left until Tuesday
            </h1>
        </div>
    )
};

export default CountDown;